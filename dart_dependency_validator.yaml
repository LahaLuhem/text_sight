# Config for `dart pub global run dependency_validator`. Scoped to the root package only
# `example/` is a separate Flutter sub-package with its own pubspec.yaml.
# Its deps shouldn't get folded into the root package's check.

exclude:
  - "example/**"
